import { mutations } from "./mutation";
import { queries } from "./query";
import { types } from "./types";
import { resolver } from "./resolver";

export const BookMark = { mutations, queries, types, resolver };
